<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" /> <!-- Color de fondo del LinearLayout -->
    <stroke
        android:width="3dp"
        android:color="@color/black" /> <!-- Color del borde (rojo) -->
    <corners android:radius="0dp" /> <!-- Esquinas redondeadas opcionales -->
</shape>